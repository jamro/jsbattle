---
  - hosts: webserver
    become: true
    become_method: sudo
    roles:
      - role: node

      - role: jsbattle
        jsbattle_port: 80
        jsbattle_address: 0.0.0.0
        jsbattle_user: root
